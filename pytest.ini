[tool:pytest][tool:pytest]

testpaths = managementtestpaths = .

python_files = test_*.pypython_files = test_*.py *_test.py api.py

python_functions = test_*python_functions = test_*

addopts = addopts = 

    -v    -v

    --tb=short    --tb=short

    --strict-markers    --strict-markers

    --disable-warnings    --disable-warnings

    --cov=.    --cov-report=term-missing

    --cov-report=term-missing    --cov-report=html:htmlcov

    --cov-report=html:htmlcov    --cov-report=xml

    --cov-report=xmlmarkers =

markers =    slow: marks tests as slow (deselect with '-m "not slow"')

    slow: marks tests as slow (deselect with '-m "not slow"')    integration: marks tests as integration tests

    integration: marks tests as integration tests    unit: marks tests as unit tests
    unit: marks tests as unit tests